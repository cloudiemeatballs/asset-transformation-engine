"use client"; import { useState } from "react"; import { Badge } from "./domain"; import { ReviewItem } from "@/research/types"; import { ReviewAction } from "@/lib/repositories/review-queue";
export function ReviewQueueClient({initialItems,persistent}:{initialItems:ReviewItem[];persistent:boolean}){const [items,setItems]=useState(initialItems),[busy,setBusy]=useState<string|null>(null),[message,setMessage]=useState("");async function decide(item:ReviewItem,action:ReviewAction){let reviewerRationale:string|undefined;if(action==="modify"||action==="reject"){reviewerRationale=window.prompt(`Why do you want to ${action} this proposal?`)??undefined;if(!reviewerRationale)return}setBusy(item.id);setMessage("");const response=await fetch("/api/review-queue",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({id:item.id,action,reviewerRationale})});const result=await response.json();if(!response.ok)setMessage(result.error??"Review failed");else{setItems(current=>current.map(x=>x.id===item.id?{...x,status:result.data.status}:x));setMessage(`Review recorded: ${result.data.status}.`)}setBusy(null)}return <><div className="filters"><select aria-label="Review category"><option>All categories</option></select><select aria-label="Review status"><option>Pending</option></select><select aria-label="Review priority"><option>Priority: all</option></select></div>{!persistent&&<div className="alert">Demo mode: connect Supabase to enable durable review decisions and canonical assessment creation.</div>}<div className="stack section">{items.map(i=><div className="panel row-between" key={i.id}><div><div className="tags"><Badge tone={i.priority==="critical"?"fail":i.priority==="high"?"hold":"neutral"}>{i.priority}</Badge><Badge>{i.category.replaceAll("_"," ")}</Badge><Badge tone={i.status==="pending"?"hold":"pass"}>{i.status}</Badge></div><h3 style={{marginTop:10,fontSize:16}}>{i.title}</h3><p className="small">{i.summary}</p></div><div className="tags">{(["accept","modify","reject","defer"] as ReviewAction[]).map(action=><button key={action} disabled={!persistent||busy===i.id||i.status!=="pending"} onClick={()=>decide(i,action)} className={`badge ${action==="accept"?"pass":action==="reject"?"fail":action==="defer"?"hold":"neutral"}`}>{busy===i.id?"SAVING…":action.toUpperCase()}</button>)}</div></div>)}</div>{message&&<p role="status" className="alert section">{message}</p>}</>}
