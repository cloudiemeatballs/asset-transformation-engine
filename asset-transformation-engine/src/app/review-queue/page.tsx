import { reviewItems } from "@/research/fixtures"; import { Badge } from "@/components/domain";
export default function ReviewQueue(){return <><div className="eyebrow">Human-in-the-loop control</div><h1>Review Queue</h1><p className="lede">Research agents create proposals, never authoritative assessments. Acceptance must create a canonical record with provenance back to the proposal.</p><div className="filters"><select><option>All categories</option></select><select><option>Pending</option></select><select><option>Priority: all</option></select></div><div className="stack">{reviewItems.map(i=><div className="panel row-between" key={i.id}><div><div className="tags"><Badge tone={i.priority==="critical"?"fail":i.priority==="high"?"hold":"neutral"}>{i.priority}</Badge><Badge>{i.category.replaceAll("_"," ")}</Badge></div><h3 style={{marginTop:10,fontSize:16}}>{i.title}</h3><p className="small">{i.summary}</p></div><div className="tags"><button className="badge pass">ACCEPT</button><button className="badge neutral">MODIFY</button><button className="badge fail">REJECT</button><button className="badge hold">DEFER</button></div></div>)}</div><p className="small section">Buttons demonstrate the review states in this local fixture runtime. The API requires a reviewer rationale for rejection or modification; broad auto-accept is intentionally disabled.</p></>}
