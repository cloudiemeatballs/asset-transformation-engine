import { ReviewQueueClient } from "@/components/review-queue-client"; import { reviewQueueRepository } from "@/lib/repositories/review-queue"; import { supabaseConfigured } from "@/lib/supabase/config";
export default async function ReviewQueue(){const items=await reviewQueueRepository().list();return <><div className="eyebrow">Human-in-the-loop control</div><h1>Review Queue</h1><p className="lede">Research agents create proposals, never authoritative assessments. Accepted assessment proposals create immutable human assessments in the same database transaction as the review decision.</p><ReviewQueueClient initialItems={items} persistent={supabaseConfigured()}/></>}
