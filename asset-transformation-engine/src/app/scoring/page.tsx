import { criteria, scoringLevels } from "@/data/seed"; import { Badge } from "@/components/domain";
export default function Scoring(){return <><div className="eyebrow">Asset Transformation Engine · v1.0</div><h1>Scoring manual</h1><p className="lede">Rubrics are rendered from the data-driven framework. Gate status is deterministic; confidence never changes the numeric score.</p><div className="alert">Defensibility Potential is a preliminary strategic assessment—not legal or freedom-to-operate advice.</div><div className="stack section">{criteria.map(c=><section className="panel" key={c.code}><div className="row-between"><div><h2>{c.name}</h2><p className="small">{c.description}</p></div><div className="tags"><Badge tone={c.isGate?"hold":"neutral"}>{c.isGate?"HARD GATE":"RANKING"}</Badge><Badge>{c.weight*100}% weight</Badge>{c.isGate&&<Badge>threshold ≥{c.gateThreshold} / {c.minimumConfidence}</Badge>}</div></div><div className="rubric">{scoringLevels.filter(l=>l.criterionCode===c.code).map(l=><div key={l.score}><b>{l.score}</b>{l.definition}</div>)}</div></section>)}</div></>}
