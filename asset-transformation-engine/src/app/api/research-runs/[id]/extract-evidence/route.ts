import{createEvidenceProposals}from"@/lib/repositories/evidence-proposals";import{NextResponse}from"next/server";import{z}from"zod";const schema=z.string().uuid();export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const parsed=schema.safeParse((await params).id);if(!parsed.success)return NextResponse.json({error:"Invalid research run ID"},{status:400});try{return NextResponse.json({data:await createEvidenceProposals(parsed.data)})}catch(error){const message=error instanceof Error?error.message:"Evidence extraction failed";return NextResponse.json({error:message},{status:message.includes("Authentication")?401:message.includes("Administrator")?403:400})}}
