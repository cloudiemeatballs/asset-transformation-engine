import { createResearchRun } from "@/lib/repositories/research-runs"; import { transformationSearchQuerySchema } from "@/domain/schemas"; import { NextResponse } from "next/server";
export async function POST(request:Request){const parsed=transformationSearchQuerySchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:parsed.error.flatten()},{status:400});try{return NextResponse.json({data:await createResearchRun(parsed.data)},{status:201})}catch(e){const message=e instanceof Error?e.message:"Could not create research run";return NextResponse.json({error:message},{status:message.includes("configured")?503:message.includes("Authentication")?401:400})}}
