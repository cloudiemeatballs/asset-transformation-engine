import Link from "next/link"; import { opportunities, targets, transformations } from "@/data/seed"; import { GateBadge } from "@/components/domain";
export default function Matrix(){return <><div className="eyebrow">Discovery interface</div><h1>Transformation × Target matrix</h1><p className="lede">Cells are generated from central opportunity records. Filter the mismatch landscape by biology, limitation, engineering strategy, and family breadth.</p><div className="filters"><select><option>All therapeutic areas</option></select><select><option>All constraint types</option></select><select><option>All scores</option></select><select><option>Human validation ≥4</option></select></div><div className="matrix"><table><thead><tr><th>Transformation</th>{targets.map(t=><th key={t}>{t}</th>)}</tr></thead><tbody>{transformations.map(tr=><tr key={tr}><th>{tr}</th>{targets.map(t=>{const o=opportunities.find(x=>x.transformation===tr&&x.target===t);return <td key={t} className={o?"hit":""}>{o?<Link href={`/opportunities/${o.slug}`}><GateBadge status={o.overallGateStatus}/><div style={{marginTop:5}}>{o.score??"—"}</div></Link>:"·"}</td>})}</tr>)}</tbody></table></div></>}
