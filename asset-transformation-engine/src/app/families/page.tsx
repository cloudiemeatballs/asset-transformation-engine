import Link from "next/link"; import { families } from "@/data/seed"; import { Badge } from "@/components/domain";
export default function Families(){return <><div className="eyebrow">Company formation</div><h1>Opportunity families</h1><p className="lede">A strong asset is not automatically a company. Families must demonstrate independent targets, multiple leads, repeatable engineering, and compounding capabilities.</p><div className="grid grid-2 section">{families.map(f=><Link href={`/families/${f.slug}`} className="panel" key={f.id}><div className="row-between"><div><div className="eyebrow">{f.stage.replaceAll("_"," ")}</div><h2>{f.name}</h2></div><div className="score">{f.score}<small>/100 · {f.confidence}</small></div></div><p className="small">{f.thesis}</p><div className="tags"><Badge tone={f.independentTargetCount>=4?"pass":"fail"}>{f.independentTargetCount} independent targets</Badge><Badge tone={f.tier1Count>=2?"pass":"fail"}>{f.tier1Count} Tier 1</Badge><Badge>{f.engineeringPrimitive}</Badge></div></Link>)}</div></>}
