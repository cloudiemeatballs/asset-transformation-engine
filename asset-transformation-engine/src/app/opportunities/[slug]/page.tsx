import { notFound } from "next/navigation"; import { opportunities as fixtureOpportunities, criteria } from "@/data/seed"; import { Badge, GateBadge, GateCards } from "@/components/domain";
export function generateStaticParams(){return fixtureOpportunities.map(o=>({slug:o.slug}))}
const opportunities=fixtureOpportunities;
export default async function Detail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const o=opportunities.find(x=>x.slug===slug);if(!o)notFound();return <><div className="row-between"><div><div className="eyebrow">{o.therapeuticArea} · {o.target}</div><h1>{o.name}</h1><div className="tags"><Badge>{o.stage.replaceAll("_"," ")}</Badge><Badge>{o.recommendation}</Badge><GateBadge status={o.overallGateStatus}/>{o.familyName&&<Badge>{o.familyName}</Badge>}</div></div><div>{o.score!==null?<div className="score">{o.score}<small>/100 · {o.confidence}</small></div>:<div className="alert"><b>Opportunity score withheld</b><br/>All six hard gates must pass.</div>}</div></div><section className="section panel thesis"><div className="eyebrow">Mandatory transformation thesis · {o.thesis.complete?"complete":"incomplete"}</div>{o.thesis.renderedText??"Required structured fields are missing."}</section><section className="section"><div className="section-head"><h2>Scientific gates</h2><div className="small">Score / confidence / deterministic state</div></div><GateCards o={o}/></section><section className="section"><div className="section-head"><h2>Evidence chain</h2></div><div className="chain">{[["Validated mechanism",o.mechanism,"supported"],["Clinical constraint",o.constraint,"supported"],["Causal property",o.causalMechanism,"partially supported"],["Intervention",o.transformation,"supported"],["Altered property",o.drugProperty,"supported"],["Pharmacology","Matched exposure at intended site","unknown"],["Clinical advantage",o.clinicalAdvantage,"partially supported"]].map(([a,b,c])=><div className="chain-node" key={a}><div className="eyebrow">{a}</div><b>{b}</b><div className="small">{c}</div></div>)}</div></section><div className="section detail-grid"><div className="stack"><section className="panel"><h2>Architecture</h2>{o.architecture.map((c,i)=><div className="row-between" style={{paddingTop:12}} key={i}><div><b>{c.name}</b><div className="small">{c.description}</div></div><Badge>{c.type}</Badge></div>)}</section><section className="panel"><h2>Evidence room</h2>{o.evidence.map(e=><div style={{paddingTop:14}} key={e.id}><div className="row-between"><Badge tone={e.relationship==="supports"?"pass":e.relationship==="contradicts"?"fail":"neutral"}>{e.relationship}</Badge><span className="small mono">L{e.evidenceLevel} · {e.extractionConfidence} · {e.humanVerified?"HUMAN VERIFIED":"AI / UNVERIFIED"}</span></div><p>{e.claimText}</p><div className="small">{e.source}</div></div>)}</section><section className="panel"><h2>Scoring breakdown</h2><table style={{marginTop:12}}><tbody>{criteria.map(c=>{const a=o.assessments.find(x=>x.criterionCode===c.code)!;return <tr key={c.code}><td><b>{c.name}</b><div className="small">{c.isGate?"Hard gate":"Post-gate ranking"} · weight {c.weight*100}%</div></td><td className="mono">{a.score}/5 · {a.confidence}</td></tr>})}</tbody></table></section><section className="panel"><h2>Counter-thesis</h2><p><b>Strongest argument</b><br/><span className="small">{o.strongestSupportingArgument}</span></p><p><b>Strongest objection</b><br/><span className="small">{o.strongestDisconfirmingArgument}</span></p><p><b>Why might we be wrong?</b><br/><span className="small">{o.whyMightWeBeWrong}</span></p></section></div><aside className="stack"><section className="panel"><h2>Killer assumptions</h2>{o.assumptions.map(a=><div style={{paddingTop:14}} key={a.id}><div className="row-between"><Badge tone={a.criticality==="fatal"?"fail":"hold"}>{a.criticality}</Badge><span className="small mono">{a.confidence} · {a.status}</span></div><p>{a.statement}</p><div className="small"><b>Kill test:</b> {a.bestTest}</div></div>)}</section><section className="panel"><h2>Next best evidence</h2>{o.diligenceActions.map(d=><div style={{paddingTop:14}} key={d.id}><Badge tone="hold">Priority {d.priority}</Badge><p>{d.question}</p><div className="small">{d.actionType} · {d.expectedInformationGain}</div></div>)}</section><section className="panel"><h2>History</h2><div className="timeline" style={{marginTop:16}}>{o.transitions.map((t,i)=><div key={i}><b>{t.to.replaceAll("_"," ")}</b><div className="small">{t.reason}<br/>{t.triggeredBy}</div></div>)}</div></section></aside></div></>}
