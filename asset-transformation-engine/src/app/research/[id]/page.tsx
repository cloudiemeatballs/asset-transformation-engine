import Link from "next/link";import {notFound} from "next/navigation";import {Badge} from "@/components/domain";import {getResearchRunDetail} from "@/lib/repositories/research-runs";
export const dynamic="force-dynamic";
export default async function ResearchRunPage({params}:{params:Promise<{id:string}>}){const {id}=await params;let run;try{run=await getResearchRunDetail(id)}catch{notFound()}const publications=run.tasks.flatMap(task=>task.publications);return <><div className="eyebrow">Research Run</div><div className="row-between"><div><h1 className="mono">{run.id}</h1><p className="lede">{run.summary}</p></div><Badge tone={run.status==="complete"?"pass":"hold"}>{run.status}</Badge></div><section className="section grid grid-3"><div className="panel"><div className="eyebrow">Tasks</div><div className="metric">{run.tasks.filter(t=>t.status==="complete").length}/{run.tasks.length}</div></div><div className="panel"><div className="eyebrow">Publications</div><div className="metric">{publications.length}</div></div><div className="panel"><div className="eyebrow">Provider</div><div className="metric" style={{fontSize:18}}>{String(run.modelConfiguration.provider??"unconfigured")}</div></div></section><section className="section"><div className="section-head"><h2>Task trace</h2><Link href="/research">All runs →</Link></div><table><thead><tr><th>Agent</th><th>Objective</th><th>Status</th><th>Attempt</th><th>Output</th></tr></thead><tbody>{run.tasks.map(task=><tr key={task.id}><td className="mono">{task.agentType}</td><td>{task.objective}</td><td><Badge tone={task.status==="complete"?"pass":task.error?"fail":"hold"}>{task.status}</Badge></td><td className="mono">{task.attemptCount}</td><td className="small">{task.output?`${String(task.output.recordsFound??0)} found · ${String(task.output.recordsImported??0)} imported`:task.error??"Awaiting execution"}</td></tr>)}</tbody></table></section><section className="section"><div className="section-head"><h2>Imported publications</h2><span className="small">Normalized provider metadata; no claims inferred.</span></div><div className="stack">{publications.map(publication=><article className="panel" key={publication.id}><div className="row-between"><div><a href={publication.providerUrl} target="_blank" rel="noreferrer"><b>{publication.title}</b></a><div className="small">{publication.publicationDate??"Date unavailable"} · PMID {publication.pmid} · normalized {publication.normalizationVersion}</div></div><Badge>PubMed</Badge></div><p className="small">{publication.authors.join(", ")||"Authors unavailable"}</p>{publication.doi&&<a className="small mono" href={`https://doi.org/${publication.doi}`} target="_blank" rel="noreferrer">DOI {publication.doi}</a>}</article>)}{!publications.length&&<div className="panel small">No publications have been imported for this run.</div>}</div></section></>}
