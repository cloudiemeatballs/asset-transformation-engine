import { QueryBuilder } from "@/components/query-builder";
export default function Query(){return <><div className="eyebrow">Structured opportunity engine</div><h1>Build a transformation search</h1><p className="lede">Narrow the north-star query with structured filters. Natural language is presentation; validated structured state is canonical.</p><QueryBuilder/></>}
