import{opportunities}from"@/data/seed";import{supabaseConfigured}from"@/lib/supabase/config";import{createSupabaseServerClient}from"@/lib/supabase/server";
export type CanonicalEvidenceClaim={id:string;claimText:string;claimType:string;species:string|null;evidenceLevel:number;directness:string;sourceLocator:string|null;sourceExcerpt:string|null;extractionConfidence:string;humanVerified:boolean;createdAt:string;source:{title:string;url:string|null;pmid:string|null;doi:string|null;publicationDate:string|null}};
type EvidenceRow={id:string;claim_text:string;claim_type:string;species:string|null;evidence_level:number;directness:string;source_locator:string|null;source_excerpt:string|null;extraction_confidence:string;human_verified:boolean;created_at:string;source:{title:string;url:string|null;pmid:string|null;doi:string|null;publication_date:string|null}|null};
export function mapEvidenceRow(row:EvidenceRow):CanonicalEvidenceClaim{return{id:row.id,claimText:row.claim_text,claimType:row.claim_type,species:row.species,evidenceLevel:row.evidence_level,directness:row.directness,sourceLocator:row.source_locator,sourceExcerpt:row.source_excerpt,extractionConfidence:row.extraction_confidence,humanVerified:row.human_verified,createdAt:row.created_at,source:{title:row.source?.title??"Unknown source",url:row.source?.url??null,pmid:row.source?.pmid??null,doi:row.source?.doi??null,publicationDate:row.source?.publication_date??null}}}
export async function listCanonicalEvidence():Promise<CanonicalEvidenceClaim[]>{if(!supabaseConfigured())return opportunities.flatMap(opportunity=>opportunity.evidence.map(claim=>({id:claim.id,claimText:claim.claimText,claimType:"illustrative_fixture",species:null,evidenceLevel:claim.evidenceLevel,directness:"illustrative",sourceLocator:null,sourceExcerpt:null,extractionConfidence:claim.extractionConfidence,humanVerified:claim.humanVerified,createdAt:"2026-08-09T00:00:00Z",source:{title:claim.source,url:null,pmid:null,doi:null,publicationDate:null}})));const db=await createSupabaseServerClient();const{data,error}=await db.from("evidence_claims").select("id,claim_text,claim_type,species,evidence_level,directness,source_locator,source_excerpt,extraction_confidence,human_verified,created_at,source:sources(title,url,pmid,doi,publication_date)").eq("human_verified",true).order("created_at",{ascending:false});if(error)throw new Error(error.message);return(data??[]).map(row=>mapEvidenceRow(row as unknown as EvidenceRow))}
