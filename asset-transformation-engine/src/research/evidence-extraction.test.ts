import{describe,expect,it}from"vitest";import{extractAbstractClaims}from"./evidence-extraction";describe("abstract claim extraction",()=>{it("creates bounded source-located candidates without rewriting text",()=>{const text="This sufficiently long sentence reports a measured clinical response in patients. Another sufficiently long sentence describes the observed safety outcome.";const claims=extractAbstractClaims(text);expect(claims[0].claimText).toBe("This sufficiently long sentence reports a measured clinical response in patients.");expect(claims[0].sourceLocator).toBe("PubMed abstract sentence 1");expect(claims[0].species).toBe("human")})});
